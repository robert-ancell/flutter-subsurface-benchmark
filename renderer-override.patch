diff --git a/engine/src/flutter/shell/platform/linux/fl_view.cc b/engine/src/flutter/shell/platform/linux/fl_view.cc
index 5c1492b1c76..88eeac7b54d 100644
--- a/engine/src/flutter/shell/platform/linux/fl_view.cc
+++ b/engine/src/flutter/shell/platform/linux/fl_view.cc
@@ -653,8 +653,19 @@ static void setup_engine(FlView* self) {
           fl_view_renderer_software_new(self->engine, self->sized_to_content));
       break;
     case kOpenGL:
-    default:
-      if (GDK_IS_WAYLAND_DISPLAY(gtk_widget_get_display(GTK_WIDGET(self)))) {
+    default: {
+      // Benchmark-only: allow forcing a renderer so the subsurface and GTK
+      // OpenGL paths can be compared using a single engine build. Do not land.
+      const gchar* forced_renderer = g_getenv("FLUTTER_LINUX_VIEW_RENDERER");
+      gboolean use_subsurface =
+          GDK_IS_WAYLAND_DISPLAY(gtk_widget_get_display(GTK_WIDGET(self)));
+      if (forced_renderer != nullptr) {
+        use_subsurface = g_str_equal(forced_renderer, "subsurface");
+      }
+      // Printed so each benchmark run can verify which renderer it used.
+      g_printerr("FLUTTER_VIEW_RENDERER=%s\n",
+                 use_subsurface ? "subsurface" : "opengl");
+      if (use_subsurface) {
         self->renderer = FL_VIEW_RENDERER(fl_view_renderer_subsurface_new(
             self->engine, self->sized_to_content));
       } else {
@@ -662,6 +673,7 @@ static void setup_engine(FlView* self) {
             fl_view_renderer_opengl_new(self->engine, self->sized_to_content));
       }
       break;
+    }
   }
   gtk_widget_show(GTK_WIDGET(self->renderer));
   gtk_container_add(GTK_CONTAINER(self->event_box), GTK_WIDGET(self->renderer));
